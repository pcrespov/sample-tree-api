#
#
# TODO: plugin for pylint
# TODO: this setup shall collect all outputs in bin and produce a wheel

#  export LD_LIBRARY_PATH=$BUILD_DIR/_bin
#  export PYTHON_APTH=$BUILD_DIR/_bin

from setuptools import find_packages, setup

setup(
    name='py-smash',
    version='0.1.0',
    python_requires='~=3.6',
    description='Minimal python package on supermash',
    url='https://zmt.swiss/sim4life/',
    packages=find_packages(where='src'),
    package_dir={
        '': 'src',
    },
    zip_safe=False
)
