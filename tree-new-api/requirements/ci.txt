# Shortcut to install all packages for the contigous integration (CI) of 'services/tree-new-api'
#
# - As ci.txt but w/ tests
#
# Usage:
#   pip install -r requirements/ci.txt
#

# installs base + tests requirements
-r _test.txt

../site-packages/py-smash

# installs current package
.
